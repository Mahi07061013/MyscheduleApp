import SwiftUI

struct FocusView: View {
    var body: some View {
        VStack {
            Text("Pomodoro Timer")
                .font(.largeTitle)
        }
    }
}

#Preview {
    FocusView()
}
