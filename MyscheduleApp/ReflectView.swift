import SwiftUI

struct ReflectView: View {
    var body: some View {
        VStack {
            Text("Analytics")
                .font(.largeTitle)
        }
    }
}

#Preview {
    ReflectView()
}
